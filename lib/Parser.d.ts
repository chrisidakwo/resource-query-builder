export default class Parser {
    private query;
    uri: string;
    constructor(query: any);
    parse(): string;
    private prepend;
    /**
     * Parsers
     */
    private includes;
    private appends;
    private fields;
    private filters;
    private sorts;
    private page;
    private limit;
    private params;
}
